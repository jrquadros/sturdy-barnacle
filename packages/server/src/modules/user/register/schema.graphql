# import Error from '../../shared.graphql'

type Query {
  hello(name: String): String!
}

type Error {
  path: String!
  message: String!
}

type Mutation {
  register(email: String!, name: String!): [Error!]
}
